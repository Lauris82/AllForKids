
{% extends '::LayoutBack.html.twig' %}
{% block title %}
    Liste des èvenements
{% endblock title %}

{% block body %}
<div id="page-wrapper" >
    <div id="page-inner">

<div class="row">
    <div class="col-md-12">
        <!-- Advanced Tables -->
        <div class="panel panel-default">
            <div class="panel-heading">
            <h2 style="color: red" align="center">    Membres Evenement</h2>
            </div>
            <div class="panel-body">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead>
                        <tr >
                            <th >Image</th>
                            <th>Nom</th>
                        <th>Prenom</th>
                            <th>Date Reservation</th>

                        </tr>
                        </thead>
                        <tbody>
                        {% for m in membres %}
                        <tr class="odd gradeX">
                            <td><img src="{{ asset('image_user/' ~ m.user.image) }}" style="   border: 1px black solid;
                                        border-radius: 50%;
                                        width: 75px;
                                        height: 75px;
                                        background:  200px;"></td>
                            <td><a href="">{{ m.user.nom }}</a> </td>
                            <td>{{ m.user.prenom }}</td>
                            <td class="center">{{ m.dateReservation| date('Y/m/d') }}</td>

                        </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock body %}




